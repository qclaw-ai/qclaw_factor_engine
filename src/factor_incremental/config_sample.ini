[factor_incremental]
; 由增量编排层调用的 factor_engine 配置文件
factor_engine_config_file = src/factor_engine/config.ini

; 运行模式：incremental / rebase
mode = incremental

; 写入 factor_value_files 的阶段
stage = candidate

; 预热交易日（用于滚动类算子，实际计算会向前多取）
warmup_trading_days = 200

