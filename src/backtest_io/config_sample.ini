[database]
db_host = 127.0.0.1
db_port = 5432
db_user = postgres
db_password = your_password_here
db_name = qclaw_factor_engine

[paths]
; 回测结果 JSON 输出目录（实际文件会写到 backtest_results/by_universe/{UNIVERSE}/）
backtest_results_dir = backtest_results

; sync_factor_values_path_runner：从 factor_value_files 回写 factor_files.factor_values_path 时使用的域
sync_factor_values_execution_universe = ALL

